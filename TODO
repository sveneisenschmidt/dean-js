TODO

- default params should used when a route should match and params are missing
- before hook options only.path can have multiple paths through an array consisting of strings and regexes
- exclude option for before, after, around hooks
- implement error logger
- implement post, put, delete
- implement options (throw_errors etc)

DONE

- integrate mustache.js // via helper Mustache.to_html
- use native hashchanged event or interval // removed interval, now only a grade browser supported
- add an fully configured application context to before
- implement before, after, around
- before hooks can stop the process
- implement on .load onFailure callback